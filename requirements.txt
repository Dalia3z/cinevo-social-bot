# Cinevo AI Social Media Engagement Bot - Python dependencies
#
# Install with:  pip install -r requirements.txt

# HTTP client used for all platform APIs and the DeepSeek API.
requests>=2.31.0

# Loads secrets from the .env file.
python-dotenv>=1.0.0

# Optional: official DeepSeek/OpenAI-compatible client.
# The bot uses plain `requests` by default, but you can enable this if you
# prefer the official client. Uncomment to install:
# openai>=1.30.0

# Optional: browser automation for TikTok/Quora (HIGH RISK, opt-in only).
# Only needed if you set TIKTOK_BROWSER_AUTOMATION=true or
# QUORA_BROWSER_AUTOMATION=true. Uncomment to install:
# selenium>=4.20.0

# Short-form VIDEO system (content_auto.py / video_maker.py).
# FFmpeg itself is a SYSTEM binary (apt-get install -y ffmpeg), not a pip
# package. The Python deps below are optional helpers:
#   * edge-tts  -> free AI voiceover for the videos.
#   * Pillow    -> image helpers (optional; not required for text shorts).
# Uncomment to install:
# edge-tts>=6.1.0
# Pillow>=10.0.0

# Optional: only needed by the local helper `set_github_secrets.py`, which
# encrypts and uploads the YouTube OAuth secrets to GitHub in one step.
# Uncomment to install:
# pynacl>=1.5.0
